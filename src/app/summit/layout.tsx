export const metadata = {
  title: 'About | REAIM 2027',
  description: 'History, significance, and co-hosts of the REAIM Summit.',
};

export default function AboutLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}

